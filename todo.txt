parents-core(cfg-server)# recursion access-class recur                         
parents-core(cfg-server)#no recursion access-class recur                       
% unknown config command entered
parents-core(cfg-server)#       
parents-core(cfg-server)# resolver facebook.com 10.10.10.255                   
parents-core(cfg-server)#no resolver facebook.com 10.10.10.255                 
% unknown config command entered
parents-core(cfg-server)#   
lsrp, ospf, isis must list lsas/lsps as reachable or not
padup ethertype config option
http host upload optional different authentication
display traceroute with the same parameters
configurable replay window under ipsec profile for macsec
macsec bad type in hex
lossdet with class util.dampening
reapplying the same vrf to interface as noop
hwdet default to binary in installation, rework rtr.bin support to binjar if needed
hwdet detect mac addresses
attach vdc name tabbing
show interfaces --> show interface
interactive switch for traceroute to mimic mtr with class util.dampening
client domain-name to resolve internally first
call-home through irc with configurable proxyprofile, read-write access
irc server should ping periodically the clients
access-* under http host
tls1.3
http2.0
more configurable defaults to lines: tablemode, timestamps, configure up them on main rtrs
route-map/policy to match private-as origination/transit, then show, ignore, etc
sorted, uniq | filter for any column
diff n | filter
terminal mode resolve
hostwatch to execute script on acl match
protocol traceroute
optimal route reflection
bgp vpns with sr-policy
recursive static route
pipeline properties, terminal autoresize
temper: web access log
clean up javadoc errors
changelog converter for https://manpages.debian.org/testing/dpkg-dev/deb-changelog.5.en.html format with notodo lines
yang over netconf, proto over grpc
interface range command
reenable which elliptic courves? sure? why that?
https://tools.ietf.org/html/draft-ietf-tn3270e-telnet-tls-06
parse privilege levels from radius and tacacs
ldap client, server
bgp compare should write out that which attributes changed
bfd need to send with ttl=255
trustpoint for certificates
interface name tabbing
int eth1/int eth2
dhcp relay support
tcp selective ack
xot, x75 switching
preallocate qos buffers
ilnp (rfc6740)
lisp4, lisp6
l2vpn for isis
hip (rfc4423)
ttl handling follow the selected forwarder, reconfigurable if more
more show commands
more clear commands
more interactive tools
more features with clean implementation
igmp4/6 snoop, pim4/6 snoop for bridging
multilink ppp, fr
x25
default vrf for show commands
